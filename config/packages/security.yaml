security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\Compte
                property: email

    firewalls:
        dev:
            # Ensure dev tools and static assets are always allowed
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: app_user_provider
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true
            logout:
                path: app_logout
                # where to redirect after logout
                # target: app_any_route

            # Activate different ways to authenticate:
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Hiérarchie des rôles — alignée sur la matrice des droits (CDC §2.2).
    # Le Personnel a tous les droits des autres profils (espace étudiant, espace enseignant,
    # gestion des offres/projets comme une entreprise) + administration du contenu (exclusif).
    role_hierarchy:
        ROLE_PERSONNEL: [ROLE_ENSEIGNANT, ROLE_ENTREPRISE, ROLE_ETUDIANT]
        ROLE_ENSEIGNANT: [ROLE_USER]
        ROLE_ENTREPRISE: [ROLE_USER]
        ROLE_ETUDIANT: [ROLE_USER]

    # Note: Only the *first* matching rule is applied
    access_control:
        - { path: ^/espace/personnel, roles: ROLE_PERSONNEL }
        - { path: ^/espace/enseignant, roles: ROLE_ENSEIGNANT }
        # - { path: ^/espace/etudiant, roles: ROLE_ETUDIANT }
        # - { path: ^/profile, roles: ROLE_USER }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
